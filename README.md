
# Chili Catcher v1.4.0

Robuste Bestenliste für Spieler:
- Erst Sockets mit Ack.
- Fallback: ChatMessage-Whisper an GMs mit Flags.
- GM hört `createChatMessage`, speichert und löscht die Systemnachricht.
- Dedupe via `requestId`.

Musik bleibt lokal. Start-/Game-Over-Screens bleiben.
